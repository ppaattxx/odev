<odoo>
    <data>
        <!-- Inherit Library Book Form View -->
        <record id="view_library_book_form_inherit" model="ir.ui.view">
            <field name="name">library.book.form.inherit</field>
            <field name="model">library.book</field>
            <field name="inherit_id" ref="library.library_book_view_form"/>
            <field name="arch" type="xml">
                <field name="name" position="after">
                    <field name="author_id"/>
                    <field name="category_id" widget="many2many_tags"/>
                </field>
            </field>
        </record>

        <!-- Inherit Library Book List View -->
        <record id="view_library_book_tree_inherit" model="ir.ui.view">
            <field name="name">library.book.tree.inherit</field>
            <field name="model">library.book</field>
            <field name="inherit_id" ref="library.library_book_view_tree"/>
            <field name="arch" type="xml">
                <field name="name" position="after">
                    <field name="author_id"/>
                    <field name="category_id" widget="many2many_tags"/>
                </field>
            </field>
        </record>
    </data>
</odoo>